{
  "name": "hav-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "dejavu-sans-condensed": "^1.0.0",
    "ptsans-npm-webfont": "0.0.4",
    "react": "^15.3.1",
    "react-dom": "^15.3.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel": "./node_modules/.bin/babel-node --presets es2015",
    "webpack": "npm run babel -- ./node_modules/.bin/webpack --progress",
    "build": "NODE_ENV=production npm run webpack",
    "build-dev": "NODE_ENV=local npm run webpack",
    "dev": "NODE_ENV=local npm run babel -- ./node_modules/.bin/webpack --progress --watch",
    "hmr": "NODE_ENV=local-hmr npm run babel -- ./node_modules/.bin/webpack-dev-server --progress --hot"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "webpack": "^2.1.0-beta.25"
  }
}
