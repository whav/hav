{
  "name": "hav-theme",
  "version": "1.0.0",
  "description": "Tailwind for hav",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tailwindcss --postcss --watch --jit -i ./src/styles.css -o ./dist/styles.css",
    "build": "NODE_ENV=production tailwindcss --postcss --jit -i ./src/styles.css -o ./dist/styles.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/forms": "^0.3.3",
    "@tailwindcss/typography": "^0.4.1",
    "autoprefixer": "^10.3.1",
    "esbuild": "^0.12.16",
    "postcss": "^8.3.6",
    "tailwindcss": "^2.2.7",
    "webpack": "^5.47.0",
    "webpack-cli": "^4.7.2"
  },
  "dependencies": {
    "postcss-import": "^14.0.2"
  }
}
