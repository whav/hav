version: "3"
services:
  nginx:
    image: nginx:mainline-alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf.template:ro
    environment:
      - IMAGESERVER_SECRET
    ports:
      - "9000:80"
    command: /bin/sh -c "sed \"s/IMAGESERVER_SECRET/$IMAGESERVER_SECRET/g\" /etc/nginx/nginx.conf.template > /etc/nginx/nginx.conf && nginx -g 'daemon off;'"


  imaginary:
    image: h2non/imaginary:latest
    volumes:
      - ${INCOMING_FILES_ROOT}:/mnt/incoming:ro
      - ${HAV_ARCHIVE_PATH}:/mnt/archive:ro
    command: -enable-url-source -mount /mnt/ -enable-placeholder