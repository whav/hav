{
  "name": "hav-theme",
  "version": "1.0.0",
  "description": "Tailwind for hav",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tailwindcss   --watch --jit -i ./src/styles.css -o ./dist/styles.css",
    "build": "NODE_ENV=production tailwindcss -i ./src/styles.css -o ./dist/styles.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.3.1",
    "postcss": "^8.3.6",
    "tailwindcss": "^2.2.7",
    "@tailwindcss/forms": "^0.3.3",
    "@tailwindcss/typography": "^0.4.1"
  }
}
